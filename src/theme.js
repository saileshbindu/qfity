import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    green: "#34C94B",
    black: "#19857b",
    white: "#ffffff",
    display:"flex",
  },
  
});

export default theme;
